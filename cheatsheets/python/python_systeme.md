OS

os.listdir() -> permet de faire un ls
os.path.isfile() -> True si le fichier existe
os.path.getsize() -> donne la taille de l'arg fichier ou dossier donc en octet = byte
os.path.join() -> fait un .join classique mais pour des paths
os.path.expanduser() -> converti un chemin string en chemin path un pointeur en gros
	tuto sur recent.py


PYTHON CLASSIQUE
rien pour l'instant
